---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-iscsi-code-server-data
  labels:
    app: code-server
spec:
  storageClassName: freenas-iscsi-manual-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  volumeName: pv-iscsi-code-server-data
