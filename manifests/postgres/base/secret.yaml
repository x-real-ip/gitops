apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgresql-secret
  labels:
    app: postgresql-database
spec:
  encryptedData:
    # Placeholder — not real ciphertext. Regenerate for your app + namespace, see README.md.
    POSTGRES_PASSWORD: "CHANGEME_run_kubeseal_for_this_app_and_namespace"
  template:
    type: Opaque
    metadata:
      name: postgresql-secret
      labels:
        app: postgresql-database
