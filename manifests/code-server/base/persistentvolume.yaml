---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: pv-iscsi-code-server-data
  labels:
    app: code-server
spec:
  storageClassName: "freenas-iscsi-manual-csi"
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: org.democratic-csi.node-manual
    readOnly: false
    fsType: xfs
    volumeHandle: pv-iscsi-code-server-data
    volumeAttributes:
      portal: truenas-master.lan.stamx.nl:3260
      iqn: iqn.2005-10.org.freenas.ctl:code-server-data
      lun: "0"
      node_attach_driver: iscsi
      provisioner_driver: node-manual
