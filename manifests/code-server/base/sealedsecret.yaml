---
kind: SealedSecret
apiVersion: bitnami.com/v1alpha1
metadata:
  name: code-server-secret
  labels:
    app: code-server
spec:
  encryptedData:
    # Placeholder — geen echte ciphertext. Verzegel je eigen wachtwoord met
    # kubeseal voor secret "code-server-secret" in namespace "tools", zie README.md.
    PASSWORD: "CHANGEME_run_kubeseal_for_this_app_and_namespace"
  template:
    metadata:
      labels:
        app: code-server
      name: code-server-secret
    type: Opaque
